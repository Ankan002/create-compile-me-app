// import gradient from "gradient-string";
// const inquirer = import("inquirer");
import { printHeading } from "utils/print-heading";

export const createProject = async () => {
	// const coolGradient = gradient(["#FFA559", "#FF6000"]);
	// const myInquirer = await inquirer;
	// const yourName = await myInquirer.default.prompt([
	// 	{
	// 		name: "name",
	// 		type: "input",
	// 		message: coolGradient("What's your name?"),
	// 	},
	// ]);
	// const yourTitle = await myInquirer.default.prompt([
	// 	{
	// 		name: "title",
	// 		type: "input",
	// 		message: coolGradient("What's your last name?"),
	// 		default: "Bhattacharya",
	// 	},
	// ]);
	// console.log(coolGradient(`My name is ${yourName.name} ${yourTitle.title}`));

	printHeading();
};
